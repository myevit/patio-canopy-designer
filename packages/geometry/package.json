{
  "name": "@canopy/geometry",
  "private": true,
  "version": "0.0.0",
  "description": "Converts validated designs into renderer-independent scene primitives.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@canopy/shared": "*",
    "@canopy/calculations": "*"
  }
}
