{
  "name": "@canopy/calculations",
  "private": true,
  "version": "0.0.0",
  "description": "Deterministic pure calculations for patio canopy structures.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@canopy/shared": "*"
  }
}
